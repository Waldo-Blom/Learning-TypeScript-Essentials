//! This is a utility file that contains a simple function to add two numbers.
//! It is exported so it can be used in other files in the project.

export function add(x:number,y:number):number {
    return x + y;
} 

